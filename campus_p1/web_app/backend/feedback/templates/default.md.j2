# {{ paper_name }} · 教学反馈报告

**班级**：{{ class_name }}　　**任课教师**：{{ teacher }}　　**日期**：{{ date }}{% if n_students %}　　**参考人数**：{{ n_students }}{% endif %}

## 一、整体表现

{{ overall_md }}

## 二、各题错题率（原始数据）

{{ questions_table_md }}

{% if weak_items %}
## 三、最薄弱 5 题

{% for q in weak_items %}
- **第 {{ q.no }} 题**（{{ q.qtype }}，错题率 {{ "%.1f"|format(q.error_rate*100) }}%，班级均分 {{ q.avg_score }}/{{ q.full_score }}）
  - 难度：{{ q.difficulty }}　典型错误：{{ q.typical_error or "—" }}
{% endfor %}
{% endif %}

{% if qtype_agg %}
## 四、按题型错题率

| 题型 | 题数 | 平均错题率 | 班级均分 |
|---|---|---|---|
{% for r in qtype_agg -%}
| {{ r.qtype }} | {{ r.count }} | {{ "%.1f"|format(r.avg_error_rate*100) }}% | {{ r.avg_score }} |
{% endfor %}
{% endif %}

{{ knowledge_md }}

{{ advice_md }}

---
*本报告由「教学反馈报告」模块自动生成，结合试卷原题（paper.json）与班级阅卷统计（Excel），并使用 LLM 生成薄弱知识点分析与教学建议。*
